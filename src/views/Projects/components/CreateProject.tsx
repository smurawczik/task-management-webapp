import Button from "@mui/material/Button";

export const CreateProject = () => {
  return (
    <Button
      size="small"
      variant="contained"
      disableElevation
      sx={{ borderRadius: 8 }}
    >
      Create Project
    </Button>
  );
};
